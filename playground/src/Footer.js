//Capital 'F' in "Footer.js" indicates that this is a React Component
import React from 'react';
import ReactDOM from 'react-dom';

function Footer() {
	return(
		//this footer is different than the 'Footer' that indicates the file name
		<footer>
			<h3>
				Test footer element
			</h3>
		</footer>
	);
}

export default Footer;